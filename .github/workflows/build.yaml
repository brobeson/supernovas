# Copyright 2023 brobeson

name: Build & Test
on:
  workflow_dispatch:
  pull_request:
  push:

# From https://stackoverflow.com/a/72408109
# Create a group from the PR number, or the branch/tag name (if not a PR).
# Cancel in-progress runs if the PR, branch, or tag is updated.
concurrency:
  group: ${{github.workflow}}-${{github.event.pull_request.number || github.ref}}
  cancel-in-progress: true

jobs:
  staticAnalysis:
    name: Support Files
    uses: brobeson/brobeson/.github/workflows/support_files.yaml@main
  # build:
  #   name: Build & Test
  #   uses: brobeson/brobeson/.github/workflows/cpp.yaml@reusable_workflows
  # CodeQuality:
  #   runs-on: ubuntu-latest
  #   env:
  #     SOURCE: source
  #     BUILD: build
  #   steps:
  #     - uses: actions/checkout@v3
  #       with:
  #         path: ${{env.SOURCE}}
  #     - name: cmake-format lint action
  #       uses: puneetmatharu/cmake-format-lint-action@v1.0.4
  #       with:
  #         args: --check
  # build:
  #   name: Build and Test
  #   env:
  #     SOURCE_DIRECTORY: source
  #     BUILD_DIRECTORY: build
  #   strategy:
  #     matrix:
  #       build_type: ["Debug", "Release"]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v2
  #       with:
  #         path: ${{env.SOURCE_DIRECTORY}}
  #     - name: Generate a Build System
  #       run: >
  #         cmake -S ${SOURCE_DIRECTORY} -B ${BUILD_DIRECTORY} -D
  #         CMAKE_EXPORT_COMPILE_COMMANDS:BOOL=on -D
  #         CMAKE_BUILD_TYPE:STRING=${{matrix.build_type}}
  #     - name: Build the Software
  #       run: cmake --build ${BUILD_DIRECTORY} --parallel $(nproc)
  #     - name: Run Unit Tests
  #       working-directory: ${{env.BUILD_DIRECTORY}}
  #       run: ctest --build-config ${{matrix.build_type}} --output-on-failure
